;; extends

; TODO: if neovim ever supports line-based offset
; ((comment) @_metadata
;  . (comment)+ @injection.content
;  (#eq? @_metadata "# METADATA")
;  (#set! injection.language "yaml")
;  (#offset! @injection.content 0 1 0 0))
